"""Baselines and comparison harness for each CSP domain: greedy, simulated
annealing, beam search, and a supervised transformer baseline, benchmarked
against the EBM solver on accuracy, convergence steps, runtime, and
constraint violations (Step 13 of the design).
"""
